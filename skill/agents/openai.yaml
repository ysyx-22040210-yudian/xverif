interface:
  display_name: kverif
  short_description: 选择并使用 kverif 验证工具，含 kdebug、kcov、kbit、MCP 与 SDK-free LSF wrapper。
  default_prompt: >
    使用 kverif 选择正确的验证工具，按需阅读对应 reference，并运行或定位
    kdebug、kcov、kbit、kloc、kentry、kberif、ksva、MCP、SDK-free loop
    wrapper 或 keda-runner 工作流。
policy:
  allow_implicit_invocation: true
